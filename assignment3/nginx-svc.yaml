apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  labels:
    app: nginx
    run: my-nginx
spec:
  type: ClusterIP
  ports:
  - port: 80
    protocol: TCP
  selector:
    app: nginx
    run: my-nginx